<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required so the foreground adhan controller can stream the full adhan
         audio from its remote URL while the app is open. Without this the
         in-app playback fails silently and only the banner shows. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Device location for accurate prayer times and Qibla direction.
         Without these declared, Geolocator can never get a fix and every
         location feature silently fell back to its default city. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- Notification permissions for prayer reminders -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <!-- Lets the Quran reader keep reciting in a foreground service with
         lock screen / status bar media controls when the app is backgrounded. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>

    <!-- Advertising ID, required by Play for apps targeting Android 13+ whose
         declaration says the advertising identifier is used. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- AdMob App ID (production — Noor Guard). -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-1882227858220460~1497344642"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:enableOnBackInvokedCallback="true">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Required for flutter_local_notifications scheduled (zonedSchedule)
             notifications to actually fire when their alarm goes off. -->
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />

        <!-- Full-screen prayer alarm Activity shown over the lock screen. -->
        <activity
            android:name=".PrayerAlarmActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:launchMode="singleTask"
            android:showOnLockScreen="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/PrayerAlarmTheme" />

        <!-- Full-screen Islamic block screen shown over a blocked app during
             an active prayer block window (Firm/Hard modes). -->
        <activity
            android:name=".BlockActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:launchMode="singleTask"
            android:theme="@style/PrayerAlarmTheme" />

        <!-- Detects when a blocked app opens during prayer time. Config and
             copy come entirely from AppBlockingStore, pushed by Dart. -->
        <service
            android:name=".AppBlockerAccessibilityService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService"/>
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config"/>
        </service>

        <!-- Stops the foreground adhan when the user taps or swipes away the
             silent prayer banner. Both contentIntent and deleteIntent point here. -->
        <receiver android:name=".AdhanStopReceiver"
                  android:exported="false">
            <intent-filter>
                <action android:name="com.noorguard.app.STOP_ADHAN"/>
            </intent-filter>
        </receiver>

        <!-- Receives AlarmManager broadcasts at exact prayer times to post
             the full-screen lock screen notification. -->
        <receiver android:name=".PrayerAlarmReceiver"
                  android:exported="false" />

        <!-- Handles "I Prayed" and "Dismiss" notification action buttons. -->
        <receiver android:name=".PrayerActionReceiver"
                  android:exported="false">
            <intent-filter>
                <action android:name="com.noorguard.app.ACTION_PRAYED"/>
                <action android:name="com.noorguard.app.ACTION_DISMISS"/>
            </intent-filter>
        </receiver>

        <!-- Reschedules pending notifications after the device reboots. -->
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Keeps the app classified as foreground so prayer AlarmManager
             broadcasts and notifications keep being delivered reliably in the
             background. Shows a persistent, silent, low-priority notification
             while active. -->
        <service
            android:name=".PrayerForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps the app alive in the background so scheduled prayer-time alarms and notifications are delivered reliably."/>
        </service>

        <!-- Fires at 12:01am nightly (armed via setAlarmClock, so Doze can't
             defer it) to re-arm the day's full-screen prayer alarms from the
             natively persisted payload, without the app being opened. Explicit
             PendingIntent target — no intent-filter needed. -->
        <receiver android:name=".MidnightSchedulerReceiver" android:exported="false" />

        <!-- Restarts the prayer keep-alive service after a reboot (if it was
             running before), and restores the day's remaining prayer alarms
             plus the nightly rescheduler wiped by the restart. -->
        <receiver android:name=".BootReceiver" android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- just_audio_background: runs the Quran reciter playback as a
             foreground media service with lock screen / status bar controls. -->
        <service
            android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <receiver
            android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <!-- Home-screen widgets (lib/services/widget_data_service.dart pushes their data). -->
        <receiver
            android:name=".widget.SmallPrayerWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_small_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/small_prayer_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.MediumPrayerWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_medium_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/medium_prayer_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.LargePrayerWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_large_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/large_prayer_widget_info" />
        </receiver>

    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Lets getInstalledApps() enumerate launchable apps for the App
             Blocking picker without the broader QUERY_ALL_PACKAGES permission. -->
        <intent>
            <action android:name="android.intent.action.MAIN"/>
            <category android:name="android.intent.category.LAUNCHER"/>
        </intent>
    </queries>
</manifest>
