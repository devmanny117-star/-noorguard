package com.noorguard.noor_guard.widget

import androidx.compose.ui.graphics.Color

/** Shared palette for all home-screen widgets, matching the in-app design
 * (navy background, gold accents, cream text). */
object WidgetTheme {
    val navy = Color(0xFF0D1B2A)
    val gold = Color(0xFFC9A84C)
    val cream = Color(0xFFF5EFE6)
    val green = Color(0xFF4CAF50)
    val mutedCream = Color(0x99F5EFE6)
}
